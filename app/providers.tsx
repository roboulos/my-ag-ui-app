"use client";

// AG UI Providers - no longer using CopilotKit
// Components will be rendered directly on the main screen
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}